apiVersion: v1
kind: Service
metadata:
  name: svc-buyerapi
  annotations:
        service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  ports:
  - port: 4000
  selector:
    app: e1buyerapi
