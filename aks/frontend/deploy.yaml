---
apiVersion: v1
kind: Deployment
metadata:
  name: ws-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: ws-service
    spec:
      containers:
      - name: backendservice
        image: mcsdevopsentarch.azurecr.io/ws-service:latest
        resources:
          requests:
            cpu: "500m"
            memory: "128Mi"
          limits:
            cpu: "1000m"
            memory: "256Mi"
        volumeMounts:
        - mountPath: "/mnt/backendservice"
          name: azurelogs
        imagePullPolicy: Always
        ports:
        - containerPort: 4000
        #env:
        # - name: AZURE_STORAGE_ACCOUNT_NAME
        #   value: ""
        # - name: AZURE_STORAGE_ACCOUNT_KEY
        #   value: ""
        # - name: AZURE_STORAGE_CONTAINER_NAME
        #   value: "backendservice"
      imagePullSecrets:
      - name: myseckey
